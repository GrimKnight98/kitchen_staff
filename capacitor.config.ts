import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kitchen_staff',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
